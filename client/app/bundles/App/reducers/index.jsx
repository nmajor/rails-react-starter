import { combineReducers } from 'redux';
// import { CONSTANT } from '../constants';
// import name from './name';

const name = (state = '', action) => { return state; };

export default combineReducers({ name });
